{
  "id": null,
  "uid": "overview",
  "title": "Portfolio Monitoring",
  "timezone": "browser",
  "schemaVersion": 38,
  "version": 1,
  "refresh": "5m",
  "tags": [
    "portfolio",
    "monitoring"
  ],
  "time": {
    "from": "now-90d",
    "to": "now"
  },
  "panels": [
    {
      "id": 1,
      "title": "Portfolio Value (EUR)",
      "type": "timeseries",
      "datasource": {
        "type": "postgres",
        "uid": "portfolio-postgres"
      },
      "gridPos": {
        "h": 9,
        "w": 24,
        "x": 0,
        "y": 0
      },
      "targets": [
        {
          "refId": "A",
          "format": "time_series",
          "rawQuery": true,
          "rawSql": "SELECT $__timeGroup(snapshot_at::timestamp, '1h') AS time, SUM(value_eur) AS portfolio_value_eur FROM portfolio_value_snapshot GROUP BY 1 ORDER BY 1;"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "currencyEUR",
          "decimals": 2,
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "calcs": [
            "lastNotNull",
            "max"
          ],
          "displayMode": "table",
          "placement": "right"
        },
        "tooltip": {
          "mode": "single"
        }
      },
      "pluginVersion": "9.5.0"
    },
    {
      "id": 2,
      "title": "Unrealized PnL by Position",
      "type": "barchart",
      "datasource": {
        "type": "postgres",
        "uid": "portfolio-postgres"
      },
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 9
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH base_currency AS (\n    SELECT COALESCE((SELECT to_ccy FROM fx_rates ORDER BY date_utc DESC LIMIT 1), 'EUR') AS code\n),\nlatest_snapshot AS (\n    SELECT MAX(snapshot_at) AS snapshot_at FROM positions_snapshot\n),\nlatest_prices AS (\n    SELECT DISTINCT ON (instrument_id) instrument_id, close, currency, as_of_utc\n    FROM prices\n    ORDER BY instrument_id, as_of_utc DESC\n),\nlatest_fx AS (\n    SELECT from_ccy, to_ccy, rate\n    FROM (\n        SELECT fr.*, ROW_NUMBER() OVER (PARTITION BY from_ccy, to_ccy ORDER BY date_utc DESC) AS rn\n        FROM fx_rates fr\n    ) ranked\n    WHERE rn = 1\n),\nposition_values AS (\n    SELECT\n        ps.account_id,\n        ps.instrument_id,\n        ps.shares,\n        ps.cost_basis_eur,\n        i.symbol,\n        COALESCE(NULLIF(i.name, ''), i.symbol) AS name,\n        COALESCE(lp.currency, i.currency) AS instrument_ccy,\n        lp.close,\n        bc.code AS base_ccy,\n        CASE\n            WHEN lp.close IS NULL THEN NULL\n            WHEN COALESCE(lp.currency, i.currency) = bc.code THEN ps.shares * lp.close\n            WHEN fx.rate IS NULL THEN NULL\n            ELSE ps.shares * lp.close * fx.rate\n        END AS market_value_base,\n        CASE\n            WHEN lp.close IS NULL THEN NULL\n            WHEN COALESCE(lp.currency, i.currency) = bc.code THEN (ps.shares * lp.close) - ps.cost_basis_eur\n            WHEN fx.rate IS NULL THEN NULL\n            ELSE (ps.shares * lp.close * fx.rate) - ps.cost_basis_eur\n        END AS unrealized_pnl_base\n    FROM positions_snapshot ps\n    JOIN latest_snapshot ls ON ls.snapshot_at = ps.snapshot_at\n    JOIN instruments i ON i.instrument_id = ps.instrument_id\n    LEFT JOIN latest_prices lp ON lp.instrument_id = ps.instrument_id\n    CROSS JOIN base_currency bc\n    LEFT JOIN latest_fx fx\n      ON fx.from_ccy = COALESCE(lp.currency, i.currency)\n     AND fx.to_ccy = bc.code\n    WHERE ps.shares <> 0\n)\nSELECT symbol || COALESCE(' – ' || name, '') AS label, SUM(unrealized_pnl_base)::double precision AS value\nFROM position_values\nWHERE unrealized_pnl_base IS NOT NULL\nGROUP BY symbol, name\nORDER BY value DESC;"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "currencyEUR",
          "decimals": 2,
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              }
            ]
          }
        },
        "overrides": []
      },
      "options": {
        "displayMode": "gradient",
        "legend": {
          "displayMode": "table",
          "placement": "right"
        },
        "orientation": "horizontal"
      },
      "pluginVersion": "9.5.0"
    },
    {
      "id": 3,
      "title": "Recent Trades",
      "type": "table",
      "datasource": {
        "type": "postgres",
        "uid": "portfolio-postgres"
      },
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 9
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT t.date_time_utc AS trade_time, t.account_id, i.symbol, t.type, t.qty, t.price, t.currency, t.net_amount, t.fees FROM transactions t JOIN instruments i ON i.instrument_id = t.instrument_id ORDER BY t.date_time_utc DESC LIMIT 25;"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "mappings": [],
          "custom": {
            "align": "center"
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "trade_time"
            },
            "properties": [
              {
                "id": "unit",
                "value": "dateTime"
              },
              {
                "id": "custom.align",
                "value": "left"
              }
            ]
          },
          {
            "matcher": {
              "id": "byNames",
              "options": {
                "mode": "exclude",
                "names": [
                  "trade_time"
                ]
              }
            },
            "properties": [
              {
                "id": "custom.align",
                "value": "center"
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "footer": {
          "show": false
        }
      },
      "pluginVersion": "9.5.0"
    },
    {
      "id": 4,
      "title": "Country Exposure",
      "type": "piechart",
      "datasource": {
        "type": "postgres",
        "uid": "portfolio-postgres"
      },
      "gridPos": {
        "h": 8,
        "w": 8,
        "x": 0,
        "y": 17
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH base_currency AS (\n    SELECT COALESCE((SELECT to_ccy FROM fx_rates ORDER BY date_utc DESC LIMIT 1), 'EUR') AS code\n),\nlatest_snapshot AS (\n    SELECT MAX(snapshot_at) AS snapshot_at FROM positions_snapshot\n),\nlatest_prices AS (\n    SELECT DISTINCT ON (instrument_id) instrument_id, close, currency, as_of_utc\n    FROM prices\n    ORDER BY instrument_id, as_of_utc DESC\n),\nlatest_fx AS (\n    SELECT from_ccy, to_ccy, rate\n    FROM (\n        SELECT fr.*, ROW_NUMBER() OVER (PARTITION BY from_ccy, to_ccy ORDER BY date_utc DESC) AS rn\n        FROM fx_rates fr\n    ) ranked\n    WHERE rn = 1\n),\nposition_values AS (\n    SELECT\n        ps.account_id,\n        ps.instrument_id,\n        ps.shares,\n        ps.cost_basis_eur,\n        i.symbol,\n        COALESCE(NULLIF(i.name, ''), i.symbol) AS name,\n        i.country,\n        COALESCE(lp.currency, i.currency) AS instrument_ccy,\n        lp.close,\n        bc.code AS base_ccy,\n        CASE\n            WHEN lp.close IS NULL THEN NULL\n            WHEN COALESCE(lp.currency, i.currency) = bc.code THEN ps.shares * lp.close\n            WHEN fx.rate IS NULL THEN NULL\n            ELSE ps.shares * lp.close * fx.rate\n        END AS market_value_base\n    FROM positions_snapshot ps\n    JOIN latest_snapshot ls ON ls.snapshot_at = ps.snapshot_at\n    JOIN instruments i ON i.instrument_id = ps.instrument_id\n    LEFT JOIN latest_prices lp ON lp.instrument_id = ps.instrument_id\n    CROSS JOIN base_currency bc\n    LEFT JOIN latest_fx fx\n      ON fx.from_ccy = COALESCE(lp.currency, i.currency)\n     AND fx.to_ccy = bc.code\n    WHERE ps.shares <> 0\n)\nSELECT COALESCE(country, 'Unknown') AS label, SUM(market_value_base)::double precision AS value\nFROM position_values\nWHERE market_value_base IS NOT NULL\nGROUP BY COALESCE(country, 'Unknown')\nORDER BY value DESC;"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "currencyEUR",
          "decimals": 2,
          "mappings": []
        },
        "overrides": []
      },
      "options": {
        "displayMode": "pie",
        "legend": {
          "displayMode": "table",
          "placement": "right"
        },
        "reduceOptions": {
          "calcs": [
            "sum"
          ],
          "fields": "",
          "values": false
        }
      },
      "pluginVersion": "9.5.0"
    },
    {
      "id": 5,
      "title": "Sector Exposure",
      "type": "piechart",
      "datasource": {
        "type": "postgres",
        "uid": "portfolio-postgres"
      },
      "gridPos": {
        "h": 8,
        "w": 8,
        "x": 8,
        "y": 17
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH base_currency AS (\n    SELECT COALESCE((SELECT to_ccy FROM fx_rates ORDER BY date_utc DESC LIMIT 1), 'EUR') AS code\n),\nlatest_snapshot AS (\n    SELECT MAX(snapshot_at) AS snapshot_at FROM positions_snapshot\n),\nlatest_prices AS (\n    SELECT DISTINCT ON (instrument_id) instrument_id, close, currency, as_of_utc\n    FROM prices\n    ORDER BY instrument_id, as_of_utc DESC\n),\nlatest_fx AS (\n    SELECT from_ccy, to_ccy, rate\n    FROM (\n        SELECT fr.*, ROW_NUMBER() OVER (PARTITION BY from_ccy, to_ccy ORDER BY date_utc DESC) AS rn\n        FROM fx_rates fr\n    ) ranked\n    WHERE rn = 1\n),\nposition_values AS (\n    SELECT\n        ps.account_id,\n        ps.instrument_id,\n        ps.shares,\n        ps.cost_basis_eur,\n        i.symbol,\n        COALESCE(NULLIF(i.name, ''), i.symbol) AS name,\n        i.sector,\n        COALESCE(lp.currency, i.currency) AS instrument_ccy,\n        lp.close,\n        bc.code AS base_ccy,\n        CASE\n            WHEN lp.close IS NULL THEN NULL\n            WHEN COALESCE(lp.currency, i.currency) = bc.code THEN ps.shares * lp.close\n            WHEN fx.rate IS NULL THEN NULL\n            ELSE ps.shares * lp.close * fx.rate\n        END AS market_value_base\n    FROM positions_snapshot ps\n    JOIN latest_snapshot ls ON ls.snapshot_at = ps.snapshot_at\n    JOIN instruments i ON i.instrument_id = ps.instrument_id\n    LEFT JOIN latest_prices lp ON lp.instrument_id = ps.instrument_id\n    CROSS JOIN base_currency bc\n    LEFT JOIN latest_fx fx\n      ON fx.from_ccy = COALESCE(lp.currency, i.currency)\n     AND fx.to_ccy = bc.code\n    WHERE ps.shares <> 0\n)\nSELECT COALESCE(sector, 'Unknown') AS label, SUM(market_value_base)::double precision AS value\nFROM position_values\nWHERE market_value_base IS NOT NULL\nGROUP BY COALESCE(sector, 'Unknown')\nORDER BY value DESC;"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "currencyEUR",
          "decimals": 2,
          "mappings": []
        },
        "overrides": []
      },
      "options": {
        "displayMode": "pie",
        "legend": {
          "displayMode": "table",
          "placement": "right"
        },
        "reduceOptions": {
          "calcs": [
            "sum"
          ],
          "fields": "",
          "values": false
        }
      },
      "pluginVersion": "9.5.0"
    },
    {
      "id": 6,
      "title": "Currency Exposure",
      "type": "piechart",
      "datasource": {
        "type": "postgres",
        "uid": "portfolio-postgres"
      },
      "gridPos": {
        "h": 8,
        "w": 8,
        "x": 16,
        "y": 17
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH base_currency AS (\n    SELECT COALESCE((SELECT to_ccy FROM fx_rates ORDER BY date_utc DESC LIMIT 1), 'EUR') AS code\n),\nlatest_snapshot AS (\n    SELECT MAX(snapshot_at) AS snapshot_at FROM positions_snapshot\n),\nlatest_prices AS (\n    SELECT DISTINCT ON (instrument_id) instrument_id, close, currency, as_of_utc\n    FROM prices\n    ORDER BY instrument_id, as_of_utc DESC\n),\nlatest_fx AS (\n    SELECT from_ccy, to_ccy, rate\n    FROM (\n        SELECT fr.*, ROW_NUMBER() OVER (PARTITION BY from_ccy, to_ccy ORDER BY date_utc DESC) AS rn\n        FROM fx_rates fr\n    ) ranked\n    WHERE rn = 1\n),\nposition_values AS (\n    SELECT\n        ps.account_id,\n        ps.instrument_id,\n        ps.shares,\n        ps.cost_basis_eur,\n        i.symbol,\n        COALESCE(NULLIF(i.name, ''), i.symbol) AS name,\n        COALESCE(lp.currency, i.currency) AS instrument_ccy,\n        lp.close,\n        bc.code AS base_ccy,\n        CASE\n            WHEN lp.close IS NULL THEN NULL\n            WHEN COALESCE(lp.currency, i.currency) = bc.code THEN ps.shares * lp.close\n            WHEN fx.rate IS NULL THEN NULL\n            ELSE ps.shares * lp.close * fx.rate\n        END AS market_value_base\n    FROM positions_snapshot ps\n    JOIN latest_snapshot ls ON ls.snapshot_at = ps.snapshot_at\n    JOIN instruments i ON i.instrument_id = ps.instrument_id\n    LEFT JOIN latest_prices lp ON lp.instrument_id = ps.instrument_id\n    CROSS JOIN base_currency bc\n    LEFT JOIN latest_fx fx\n      ON fx.from_ccy = COALESCE(lp.currency, i.currency)\n     AND fx.to_ccy = bc.code\n    WHERE ps.shares <> 0\n)\nSELECT COALESCE(instrument_ccy, 'Unknown') AS label, SUM(market_value_base)::double precision AS value\nFROM position_values\nWHERE market_value_base IS NOT NULL\nGROUP BY COALESCE(instrument_ccy, 'Unknown')\nORDER BY value DESC;"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "currencyEUR",
          "decimals": 2,
          "mappings": []
        },
        "overrides": []
      },
      "options": {
        "displayMode": "pie",
        "legend": {
          "displayMode": "table",
          "placement": "right"
        },
        "reduceOptions": {
          "calcs": [
            "sum"
          ],
          "fields": "",
          "values": false
        }
      },
      "pluginVersion": "9.5.0"
    },
    {
      "id": 7,
      "title": "Open Positions",
      "type": "table",
      "datasource": {
        "type": "postgres",
        "uid": "portfolio-postgres"
      },
      "gridPos": {
        "h": 12,
        "w": 24,
        "x": 0,
        "y": 25
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH base_currency AS (\n    SELECT COALESCE((SELECT to_ccy FROM fx_rates ORDER BY date_utc DESC LIMIT 1), 'EUR') AS code\n),\nlatest_snapshot AS (\n    SELECT MAX(snapshot_at) AS snapshot_at FROM positions_snapshot\n),\nlatest_prices AS (\n    SELECT DISTINCT ON (instrument_id) instrument_id, close, currency, as_of_utc\n    FROM prices\n    ORDER BY instrument_id, as_of_utc DESC\n),\nlatest_fx AS (\n    SELECT from_ccy, to_ccy, rate\n    FROM (\n        SELECT fr.*, ROW_NUMBER() OVER (PARTITION BY from_ccy, to_ccy ORDER BY date_utc DESC) AS rn\n        FROM fx_rates fr\n    ) ranked\n    WHERE rn = 1\n),\nposition_values AS (\n    SELECT\n        ps.account_id,\n        ps.instrument_id,\n        ps.shares,\n        ps.cost_basis_eur,\n        i.symbol,\n        COALESCE(NULLIF(i.name, ''), i.symbol) AS name,\n        i.sector,\n        i.country,\n        COALESCE(lp.currency, i.currency) AS instrument_ccy,\n        lp.close,\n        lp.as_of_utc,\n        bc.code AS base_ccy,\n        CASE\n            WHEN lp.close IS NULL THEN NULL\n            WHEN COALESCE(lp.currency, i.currency) = bc.code THEN ps.shares * lp.close\n            WHEN fx.rate IS NULL THEN NULL\n            ELSE ps.shares * lp.close * fx.rate\n        END AS market_value_base,\n        CASE\n            WHEN lp.close IS NULL THEN NULL\n            WHEN COALESCE(lp.currency, i.currency) = bc.code THEN (ps.shares * lp.close) - ps.cost_basis_eur\n            WHEN fx.rate IS NULL THEN NULL\n            ELSE (ps.shares * lp.close * fx.rate) - ps.cost_basis_eur\n        END AS unrealized_pnl_base\n    FROM positions_snapshot ps\n    JOIN latest_snapshot ls ON ls.snapshot_at = ps.snapshot_at\n    JOIN instruments i ON i.instrument_id = ps.instrument_id\n    LEFT JOIN latest_prices lp ON lp.instrument_id = ps.instrument_id\n    CROSS JOIN base_currency bc\n    LEFT JOIN latest_fx fx\n      ON fx.from_ccy = COALESCE(lp.currency, i.currency)\n     AND fx.to_ccy = bc.code\n    WHERE ps.shares <> 0\n)\nSELECT\n    account_id,\n    symbol,\n    name,\n    shares::double precision AS shares,\n    instrument_ccy AS currency,\n    close::double precision AS last_price,\n    as_of_utc AS price_as_of,\n    cost_basis_eur::double precision AS cost_basis_eur,\n    market_value_base::double precision AS market_value_eur,\n    unrealized_pnl_base::double precision AS unrealized_pnl_eur,\n    CASE\n        WHEN SUM(market_value_base) OVER () > 0 THEN (market_value_base / SUM(market_value_base) OVER ())\n        ELSE NULL\n    END::double precision AS portfolio_weight,\n    sector,\n    country,\n    base_ccy AS base_currency\nFROM position_values\nWHERE market_value_base IS NOT NULL\nORDER BY market_value_base DESC;"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "mappings": [],
          "custom": {
            "align": "center"
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "price_as_of"
            },
            "properties": [
              {
                "id": "unit",
                "value": "dateTime"
              },
              {
                "id": "custom.align",
                "value": "left"
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "shares"
            },
            "properties": [
              {
                "id": "decimals",
                "value": 4
              }
            ]
          },
          {
            "matcher": {
              "id": "byNames",
              "options": {
                "mode": "include",
                "names": [
                  "cost_basis_eur",
                  "market_value_eur",
                  "unrealized_pnl_eur"
                ]
              }
            },
            "properties": [
              {
                "id": "unit",
                "value": "currencyEUR"
              },
              {
                "id": "decimals",
                "value": 2
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "last_price"
            },
            "properties": [
              {
                "id": "unit",
                "value": "currency"
              },
              {
                "id": "decimals",
                "value": 4
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "portfolio_weight"
            },
            "properties": [
              {
                "id": "unit",
                "value": "percentunit"
              },
              {
                "id": "decimals",
                "value": 2
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "footer": {
          "show": false
        }
      },
      "pluginVersion": "9.5.0"
    }
  ],
  "templating": {
    "list": []
  }
}
